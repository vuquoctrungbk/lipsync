# Local Vietnamese Lip-Sync app — Python 3.11 runtime deps.
#
# PyTorch is installed SEPARATELY (CUDA 12.1 build) before this file, via:
#   pip install torch==2.1.2 torchvision==0.16.2 torchaudio==2.1.2 \
#       --index-url https://download.pytorch.org/whl/cu121
# (kept off this list so pip does not pull the CPU build from PyPI.)
#
# Pins chosen for SadTalker (Apache-2.0) + BiRefNet (MIT) + Gradio on Python 3.11.
# The numpy/numba/librosa triad is the fragile zone — do not bump casually.

# --- build backbone (setuptools<81 keeps pkg_resources, needed by librosa 0.9.2) ---
setuptools<81

# --- numeric core (numpy<2: torch 2.1.2 + numba + opencv all built for 1.x) ---
numpy==1.23.5
numba==0.58.1
llvmlite==0.41.1
scipy==1.11.4

# --- audio (SadTalker targets librosa 0.9.x API; resampy/soundfile are its backends) ---
librosa==0.9.2
resampy==0.4.3
soundfile==0.12.1
pydub==0.25.1

# --- image / video IO (video via imageio-ffmpeg + ffmpeg CLI; PyAV not needed) ---
imageio==2.34.0
imageio-ffmpeg==0.4.9
opencv-python==4.9.0.80
scikit-image==0.21.0

# --- SadTalker compute deps ---
kornia==0.6.8
face-alignment==1.3.5
yacs==0.1.8
joblib==1.3.2
safetensors==0.4.2
pyyaml==6.0.1
einops==0.7.0
tqdm

# --- face landmark + enhancer (facexlib = core preprocess; gfpgan/basicsr imported by
#     SadTalker's animate module at load, also power the optional GFPGAN enhancer toggle).
#     basicsr 1.4.2 imports torchvision.transforms.functional_tensor -> requires torchvision<0.17. ---
facexlib==0.3.0
gfpgan==1.3.8
basicsr==1.4.2

# --- BiRefNet matting (loaded via transformers AutoModel, trust_remote_code) ---
transformers==4.38.2
timm==0.9.16
huggingface_hub==0.21.4

# --- UI ---
gradio==4.44.1
