# Local Vietnamese Lip-Sync app — Python 3.11 runtime deps.
#
# PyTorch is installed SEPARATELY (CUDA 12.1 build) before this file, via:
#   pip install torch==2.1.2 torchvision==0.16.2 torchaudio==2.1.2 \
#       --index-url https://download.pytorch.org/whl/cu121
# (kept off this list so pip does not pull the CPU build from PyPI.)
#
# Pins chosen for SadTalker (Apache-2.0) + BiRefNet (MIT) + Gradio on Python 3.11.
# The numpy/numba/librosa triad is the fragile zone — do not bump casually.

# --- build backbone (setuptools<81 keeps pkg_resources, needed by librosa 0.9.2) ---
setuptools<81

# --- numeric core (numpy<2: torch 2.1.2 + numba + opencv all built for 1.x) ---
numpy==1.23.5
numba==0.58.1
llvmlite==0.41.1
scipy==1.11.4

# --- audio (SadTalker targets librosa 0.9.x API; resampy/soundfile are its backends) ---
librosa==0.9.2
resampy==0.4.3
soundfile==0.12.1
pydub==0.25.1

# --- image / video IO (video via imageio-ffmpeg + ffmpeg CLI; PyAV not needed) ---
imageio==2.34.0
imageio-ffmpeg==0.4.9
opencv-python==4.9.0.80
scikit-image==0.21.0

# --- SadTalker compute deps ---
kornia==0.6.8
face-alignment==1.3.5
yacs==0.1.8
joblib==1.3.2
safetensors==0.4.2
pyyaml==6.0.1
einops==0.7.0
tqdm

# --- face landmark + enhancer (facexlib = core preprocess; gfpgan/basicsr imported by
#     SadTalker's animate module at load, also power the optional GFPGAN enhancer toggle).
#     basicsr 1.4.2 imports torchvision.transforms.functional_tensor -> requires torchvision<0.17. ---
facexlib==0.3.0
gfpgan==1.3.8
basicsr==1.4.2

# --- BiRefNet matting (loaded via transformers AutoModel, trust_remote_code) ---
transformers==4.38.2
timm==0.9.16
huggingface_hub==0.21.4

# --- RobustVideoMatting (default matting engine; GPL-3, personal use) ---
# No pip package: loaded via torch.hub pinned to commit
# PeterL1n/RobustVideoMatting@53d74c6826735f01f4406b5ca9075eee27bec094
# (see lipsync/matting_rvm.py). Weights cache under models/rvm on first load
# (needs internet once, ~15 MB). commercial_safe=True avoids it entirely.

# --- UI + web-serving stack ---
# gradio 4.44.1 under-constrains its HTTP deps (e.g. only fastapi<1.0), so an
# unpinned install drifts to versions that break launch: fastapi 0.13x +
# starlette 1.x flip TemplateResponse arg order -> "unhashable type: 'dict'".
# The whole serving cluster is pinned to the exact set verified to launch +
# serve with gradio 4.44.1 / gradio_client 1.3.0. (gradio_client 1.3.0's
# boolean-schema crash is handled in lipsync/gradio_schema_compat.py.)
# Do not bump piecemeal — re-verify `python app.py` reaches HTTP 200 first.
gradio==4.44.1
gradio-client==1.3.0
fastapi==0.112.2
starlette==0.38.6
uvicorn==0.49.0
pydantic==2.13.4
pydantic-core==2.46.4
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.13.0
websockets==12.0
orjson==3.11.9
python-multipart==0.0.32
aiofiles==23.2.1
jinja2==3.1.6
markupsafe==2.1.5
