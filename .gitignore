# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# Vendored third-party source (cloned at setup)
third_party/
# SyncNet validation harness working dir (cloned repo + isolated venv + weights)
tools/syncnet/
# TTS isolated venv only — tools/tts/ code itself IS tracked
tools/tts/.venv/
# Animation-engine spike workspaces (cloned repos + venvs + weights) — disposable; reports are the durable output
tools/joyvasa/
tools/ditto/
tools/latentsync/

# Model checkpoints (downloaded at setup, multi-GB)
models/
# facexlib/gfpgan runtime weight cache (CWD-relative default path)
gfpgan/

# Internal plans & reports — kept local only (per user request 2026-07-04)
plans/

# Runtime artifacts
outputs/
temp/
*.log

# Media (test inputs/outputs)
*.mp4
*.mov
*.wav
*.mp3
*.webm
!assets/**
# voices/ stays LOCAL-ONLY on purpose: it holds user clone references (real
# people's voices — PII). TTS preset voices ship inside the vieneu wheel, so
# nothing under voices/ needs committing. (Review finding 2026-07-05.)

# OS / editor
Thumbs.db
.DS_Store
.vscode/
.idea/
